# Neon Postgres — get these from your Neon project dashboard (Connection Details).
# DATABASE_URL: pooled connection (pgbouncer) — used by the app at runtime.
DATABASE_URL="postgresql://user:password@ep-example-123456-pooler.us-east-2.aws.neon.tech/sultan_mauritius?sslmode=require"
# DIRECT_URL: direct (non-pooled) connection — used by the Prisma CLI for migrations.
DIRECT_URL="postgresql://user:password@ep-example-123456.us-east-2.aws.neon.tech/sultan_mauritius?sslmode=require"
